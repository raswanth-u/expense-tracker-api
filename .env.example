# =============================================================================
# DOCKER COMPOSE - ENVIRONMENT VARIABLES TEMPLATE (DEVELOPMENT)
# =============================================================================
#
# Copy this file to .env and fill in actual values.
#
# IMPORTANT: 
#   - Never commit .env file to Git
#   - Add to .gitignore: .env
#   - chmod 600 .env (read/write by owner only)
#   - Use strong passwords: openssl rand -base64 32
#
# =============================================================================

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# PostgreSQL username
POSTGRES_USER=expense_admin

# PostgreSQL password (CHANGE THIS!)
# Generate with: openssl rand -base64 32
POSTGRES_PASSWORD=your_secure_dev_database_password_here

# PostgreSQL database name
POSTGRES_DB=expenses_db

# =============================================================================
# API CONFIGURATION
# =============================================================================

# FastAPI application API key
# Generate unique key for each environment
API_KEY=your-dev-api-key-change-me

# Database connection URL (used by API container)
# Format: postgresql://username:password@host:port/database
# Note: 'db' is the Docker Compose service name (DNS resolution)
DATABASE_URL=postgresql://expense_admin:your_secure_dev_database_password_here@db:5432/expenses_db

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================

# Environment type (development or production)
ENVIRONMENT=development

# Log level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=DEBUG

# Skip SSL certificate verification (development only)
# Set to false for production with valid certificates
SKIP_SSL_VERIFY=true

